default:
    suites:
        develop:
            paths:
                - "%paths.base%/features/apiblueprint"
            contexts:
                - FeatureContext:
                    baseUrl: http://localhost:3000/
