default:
    suites:
        core:
            paths:
                - "%paths.base%/features/core"
            contexts:
                - FeatureContext:
                    baseUrl: http://localhost:3000/
